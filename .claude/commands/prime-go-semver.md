---
description: Prime the legacy go-semver context for cross-repo work
---

# Legacy go-semver project context loader

Loads the cross-repository context from `~/code/go-semver/` repo.

## Purpose

Explore the go-semver project

### Workflow

1. cd into `~/code/go-semver/`
2. `git ls-files | head -100` using the Bash tool to see the project structure
3. Read `README.md` for project overview and rules
4. cd back into this project `~/code/semver-trust/semver-trust-go/`
8. Summarize your understanding:
    - **Purpose**:
    - **Architecture**: 
    - **Key files**:
    - **How the semver-trust specification relates to this project**
